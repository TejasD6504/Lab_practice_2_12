<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Participants</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
            color: #fff;
            min-height: 100vh;
            padding: 0;
        }

        .header {
            background: #000;
            padding: 20px 40px;
            border-bottom: 3px solid #ffd700;
            box-shadow: 0 2px 10px rgba(255, 215, 0, 0.2);
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .header h1 {
            color: #ffd700;
            font-size: 28px;
            font-weight: 600;
            letter-spacing: 1px;
        }

        .back-btn {
            background-color: #333;
            color: #ffd700;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-weight: 600;
            font-size: 14px;
            transition: all 0.2s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            text-decoration: none;
            display: inline-block;
        }

        .back-btn:hover {
            background-color: #444;
            box-shadow: 0 2px 8px rgba(255, 215, 0, 0.2);
            transform: translateY(-1px);
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .section {
            background: #1e1e1e;
            padding: 30px;
            border-radius: 8px;
            border: 1px solid #333;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid #ffd700;
        }

        .section-header h2 {
            color: #ffd700;
            font-size: 24px;
            font-weight: 600;
        }

        .participant-count {
            background: #252525;
            color: #ffd700;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
            border: 1px solid #ffd700;
        }

        .participants-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .item-card {
            background: #252525;
            border: 1px solid #3a3a3a;
            padding: 20px;
            border-radius: 6px;
            transition: all 0.2s ease;
        }

        .item-card:hover {
            border-color: #ffd700;
            box-shadow: 0 2px 8px rgba(255, 215, 0, 0.15);
            transform: translateY(-2px);
        }

        .item-card h3 {
            color: #ffd700;
            font-size: 18px;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .item-card p {
            color: #bbb;
            line-height: 1.6;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .item-card p::before {
            content: "📅";
            font-size: 16px;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #777;
        }

        .empty-state-icon {
            font-size: 64px;
            margin-bottom: 20px;
            opacity: 0.5;
        }

        .empty-state p {
            font-size: 18px;
            margin-bottom: 10px;
        }

        .empty-state small {
            font-size: 14px;
            color: #666;
        }

        @media (max-width: 768px) {
            .header {
                padding: 15px 20px;
                flex-direction: column;
                align-items: flex-start;
            }

            .header h1 {
                font-size: 22px;
            }

            .container {
                padding: 20px 15px;
            }

            .section {
                padding: 20px;
            }

            .section-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }

            .participants-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <a href="javascript:history.back()" class="back-btn">← Back</a>
        <h1>👥 Event Participants</h1>
    </div>

    <div class="container">
        <div class="section">
            <div class="section-header">
                <h2>Participants</h2>
                <% if (participant && participant.length > 0) { %>
                    <span class="participant-count"><%= participant.length %> Registered</span>
                <% } %>
            </div>

            <% if (participant && participant.length > 0) { %>
                <div class="participants-grid">
                    <% participant.forEach(p => { %>
                        <div class="item-card">
                            <h3><%= p.name %></h3>
                            <p><%= p.event %></p>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-state-icon">👥</div>
                    <p>No participants yet</p>
                    <small>Participants will appear here once they register for this event.</small>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>